// Work in progress
/*
'use strict';

describe('mgcSorter', function() {
  var element;


  afterEach(function() {
    dealoc(element);
  });


  it('should add new and remove old classes dynamically', inject(function($rootScope, $compile) {
    var html =  "<div mgcSorter>"
                + "<div>item 2</div>"
                + "<div>item 1</div>"
                + "<div>item 13</div>"
                "</div>";
    element = $compile('<div class="existing" ng-class="dynClass"></div>')($rootScope);
    $rootScope.dynClass = 'A';
    $rootScope.$digest();
    expect(element.hasClass('existing')).toBe(true);
 
    console.log("zoo = " + ( $zoo+$zoo+1));
  }));
  
});
*/
